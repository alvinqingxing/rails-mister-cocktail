<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8 message">
      <h1>What's your ingredient?</h1>

      <%= simple_form_for @ingredient do |f| %>
        <%= f.input :name %>
        <%= f.button :submit, class: 'btn btn-link' %>
      <% end %>
      <%= link_to "Back to all cocktails", root_path %></div>
    </div>
  </div>
</div>
